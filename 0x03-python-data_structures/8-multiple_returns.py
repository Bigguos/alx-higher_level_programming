#!/usr/bin/python3
def multiple_returns(sentence):
    i = len(sentence):
    if i == 0:
        new_tuple = (0, None)
    else:
        new_tuple = (len(sentencw), sentence[0])
    return new_tuple
        

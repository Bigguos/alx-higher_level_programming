#!/usr/bin/python3
def new_in_list(my_list, idx, element):
    if my_list
        my_list.insert( , )
        i = len(my_list) - 1
        if idx < 0:
            return my_list
        elif idx > i:
            return my_list
        else:
            return my_list
    

